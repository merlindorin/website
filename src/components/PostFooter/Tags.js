import styled from "styled-components"

export default styled.ul`
  font-size: 14px;
  font-weight: 600;
  display: -ms-flexbox;
  display: flex;
  -ms-flex-direction: row;
  flex-direction: row;
  max-width: 100%;
  margin: -15px 0 68px;
  padding: 0;
  color: #fff;
  -ms-flex-wrap: wrap;
  flex-wrap: wrap;
`
